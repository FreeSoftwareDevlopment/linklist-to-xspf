name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: gpp
      run: g++ -std=c++17 "linklist to playlist.xspf.cpp"
    - name: test
      run: ./a.out
